# Chrome Sidebar for OpenClaw — 产品设计文档（PRD）

> 版本：v1.0  
> 日期：2026-04-25  
> 状态：需求确认 / 待开发  

---

## 1. 产品概述

### 1.1 产品名称
**OpenClaw Sidebar**（Chrome Extension）

### 1.2 产品定位
面向个人用户与开发者的 Chrome 浏览器侧边栏插件，让用户在任意网页浏览场景中，通过 Chrome Side Panel 与自己的 OpenClaw AI Agent 进行实时对话。插件强调**低延迟、真流式、会话连续**的体验。

### 1.3 核心价值
- **随时可聊**：不离开当前页面，一键打开侧边栏即可对话。
- **真流式体验**：AI 输出以块（block）为单位实时推送，用户无需等待全文生成。
- **会话连续**：关闭侧边栏后再次打开，自动恢复上一次的会话内容与上下文。
- **安全隔离**：OpenClaw Gateway 端口不直接暴露公网，通过 Relay 中继桥接，降低攻击面。

---

## 2. 用户场景与角色

| 角色 | 场景 | 期望 |
|------|------|------|
| 开发者 | 浏览技术文档时遇到疑问，打开侧边栏询问 OpenClaw | 快速得到代码解释、示例与修复建议 |
| 产品经理 | 在 Web 端看需求文档时，让 OpenClaw 帮忙提炼要点 | 流式输出不阻塞阅读，支持 Markdown 排版 |
| 普通用户 | 任意网页场景下的日常问答 | 打开即用，对话历史不丢失，可新建话题 |

---

## 3. 大环境约束

| 项目 | 说明 |
|------|------|
| 服务器 | 阿里云 Linux 服务器，OpenClaw Gateway 运行于此 |
| 对外端口 | 当前仅放行 **18790**（由 Nginx/反向代理或 Relay Server 监听） |
| 客户端 | 用户个人 MacBook，安装 Chrome 浏览器 |
| Gateway 现状 | 绑定 `loopback:18789`，不直接暴露公网 |
| 扩展要求 | 必须符合 **Chrome Manifest V3** 规范 |

> **端口变更流程**：若后续需要新增公网端口，需由用户手动在阿里云安全组中放行，并同步更新文档。

---

## 4. 功能需求

### 4.1 侧边栏对话（核心功能）

#### FR-001 Side Panel 入口
- 插件安装后，在 Chrome 工具栏显示插件图标。
- 点击图标调用 `chrome.sidePanel.open()` 打开侧边栏。
- 支持快捷键（可配置，默认 `Alt+Shift+O`）快速打开/关闭。

#### FR-002 对话界面
- 界面为上下结构：
  - **顶部**：会话标题栏（显示当前会话名称/ID）、新建会话按钮（`+`）、设置入口。
  - **中部**：消息列表（用户消息右对齐，AI 消息左对齐），支持滚动浏览历史。
  - **底部**：输入框（支持多行文本、Enter 发送、Shift+Enter 换行）、发送按钮。
- 输入框支持粘贴图片（直接作为附件发送）。

#### FR-003 真流式输出（块模式）
- AI 回复必须以**块（block）**为单位实时渲染到界面，而非等全文返回后再展示。
- 每收到一个块，立即解析并追加到当前消息中。
- 块内容按 Markdown 实时解析渲染，支持：
  - 标题、列表、引用、代码块（含语法高亮）、表格、链接、图片。
  - 代码块需支持一键复制。
- 流式过程中，当前消息区域底部显示**闪烁光标**或**正在输入…**提示。

#### FR-004 Typing Indicator
- AI 开始处理到首个可见内容块输出之间，显示 Typing Indicator（动画点或"OpenClaw 正在思考…"）。
- Gateway 提供四种 `typingMode`，Extension 侧根据当前会话所绑定的模式进行渲染：

| typingMode | 触发时机 | 前提条件 | Extension 行为 |
|------------|----------|----------|----------------|
| `instant` | 模型循环开始（首 token 生成前） | 无 | 收到 `typing.start` 即显示 Indicator |
| `thinking` | 首个 reasoning delta 到达 | `reasoningLevel: "stream"` 已开启 | 在 `message_start` 之前收到 reasoning 内容时显示 Indicator |
| `message` | 首个非静默文本 delta（即首个对用户可见的内容块） | 无 | 在 `message_update` 携带首个可见文本时显示 Indicator |
| `never` | 不显示 | — | Extension 始终不显示 Indicator |

- **reasoningLevel 作用**：`thinking` 模式下，若会话未开启 `reasoningLevel: "stream"`，则 reasoning 内容不会以流式事件下发，Extension 无法区分"正在思考"与"空闲"，此时回退到 `instant` 模式行为。
- **typingIntervalSeconds**（默认 6s）由 Gateway 侧控制：Gateway 每隔 6 秒发送一次 `typing.start` 事件以刷新 Indicator 动画。Extension 侧无需独立维护定时器，只负责在每次收到 `typing.start` 时重置动画状态；在收到 `typing.end` 时停止。
- 刷新间隔默认 6 秒（`typingIntervalSeconds`），由 Gateway 侧控制，Extension 侧负责接收事件并渲染。

#### FR-005 会话持久化与恢复
- 用户关闭 Side Panel 后，当前会话 ID 与已加载消息缓存在 `chrome.storage.local` 中。
- 再次打开 Side Panel 时：
  1. 读取本地缓存的 `activeSessionId`。
  2. 向 Relay Server 请求该会话的历史消息（或基于本地缓存渲染）。
  3. 重新订阅该会话的流事件（`sessions.messages.subscribe`）。
  4. 若会话在服务端已被清理或失效，自动提示用户并引导新建会话。

#### FR-006 新建会话
- 点击顶部 `+` 按钮可新建会话。
- 新建后：
  - 前端生成新的 `sessionId`（UUIDv4）。
  - 通过 Relay 向 Gateway 发送 `session.create`。
  - 成功后清空当前消息列表，切换为新会话上下文。
  - 旧会话保留在本地历史列表中，可随时切换回来。

#### FR-007 会话历史列表
- 支持查看近期会话列表（最近 20 条）。
- 点击历史会话可切换上下文并加载历史消息。
- 支持删除本地会话记录（仅清本地缓存，服务端会话保留由 Gateway 策略决定）。

#### FR-008 Markdown 渲染
- 所有 AI 输出消息必须经过 Markdown 解析器渲染。
- 代码块使用 `highlight.js` 或 `prismjs` 做语法高亮。
- 表格需支持横向滚动，避免撑破侧边栏窄屏布局。
- 图片链接需做安全检查（仅渲染 HTTPS 图片，禁止执行脚本）。

---

### 4.2 连接与认证

#### FR-009 Relay 接入
- Extension 不直接连接 OpenClaw Gateway（`127.0.0.1:18789` 不可达）。
- Extension 通过 **WSS（WebSocket Secure）** 连接到部署在阿里云上的 **Relay Server**（监听 18790 端口）。
- Relay Server 与 OpenClaw Gateway 建立内部 WebSocket 连接，完成双向转发与协议翻译。

#### FR-010 认证
- Extension 首次安装时，需输入配对码或长期 Token。
- Token 经 Relay Server 校验后，换取 Gateway 的会话凭证。
- 凭证（`token` + `relayId`）保存在 `chrome.storage.local` 中，后续自动重连使用。

---

### 4.3 异常与边界场景

| 场景 | 产品行为 |
|------|----------|
| 网络断开 | 显示"连接已断开，正在重连…"，指数退避重试（1s → 2s → 4s → 8s，最大 30s）。重连成功后恢复当前会话订阅。 |
| Gateway 重启 | Relay Server 感知断连，Extension 通过 Relay 重新执行 `connect` 握手并恢复会话。 |
| 会话失效 | 若 Gateway 返回会话不存在，提示"当前会话已过期"，引导用户新建会话。 |
| AI 输出超时 | 超过 60 秒无新块到达，显示"响应较慢，请稍候或重试"，用户可点击重发。 |
| 用户快速开关侧边栏 | 每次打开时读取本地状态，若 WebSocket 仍连接则直接复用，避免重复握手。 |
| 同时多标签页 | 各标签页 Side Panel 共享同一个 Service Worker 连接池，通过 `chrome.runtime` 消息总线通信，避免重复建连。 |

---

## 5. 非功能需求

### 5.1 性能
- 首屏打开到可输入时间 < 500ms（本地缓存命中情况下）。
- 从用户发送消息到首个流式块到达的延迟 < 1s（网络正常时）。
- 每块渲染帧率不低于 30fps，列表滚动不掉帧。

### 5.2 安全
- Extension 与 Relay Server 之间必须使用 WSS（TLS 加密）。
- Relay Server 与 Gateway 之间建议走本地回环或内网 TLS，避免凭证泄露。
- Extension 内容安全策略（CSP）禁止内联脚本，仅允许自有脚本与受控 CDN 资源。
- Token 不打印到日志，不暴露给内容脚本（Content Script）。

### 5.3 可维护性
- 代码模块化，UI、网络、存储、协议解析四层分离。
- 所有配置集中管理，支持环境变量区分开发/生产。
- 关键指标（连接数、消息延迟、错误率）可观测。

---

## 6. 交互原型要点

```
┌─────────────────────────────┐
│ ⬅ OpenClaw Sidebar      [+] │  ← 标题栏（返回/新建）
├─────────────────────────────┤
│                             │
│  👤 这段代码怎么优化？      │  ← 用户消息（右对齐）
│                             │
│  🤖 你可以尝试以下方式：    │  ← AI 消息（左对齐，Markdown）
│     1. 使用 `map` 替代...   │
│     ```js                   │
│     const arr = [1,2,3];    │
│     ```                     │
│     ▋                       │  ← 流式光标/正在输入
│                             │
├─────────────────────────────┤
│ [🎙] 请输入消息...      [➤] │  ← 输入区
└─────────────────────────────┘
```

---

## 7. 术语表

| 术语 | 说明 |
|------|------|
| Gateway | OpenClaw 消息网关，负责 AI Agent 会话管理、事件分发 |
| Relay Server | 部署在阿里云的中继服务，桥接 Extension 与 Gateway，隐藏真实端口 |
| Side Panel | Chrome 浏览器侧边栏（Manifest V3 `sidePanel` API） |
| Block / 块 | OpenClaw 流式输出中的离散内容单元，如一段文本、一个代码块 |
| Session | 一次连续的对话上下文，由 `sessionId` 唯一标识 |
| Typing Indicator | AI 正在生成回复时的视觉反馈（如动画点、文字提示） |

---

## 8. 附录：需求溯源矩阵

| 需求 ID | 来源（projectPrompt.md） |
|---------|--------------------------|
| FR-001 ~ FR-002 | 3.1 — 使用 chrome 侧边栏形式显示 extension，对话样式 |
| FR-003 | 3.2 / 2.2 / 4 — 真流式、块输出模式、系统交互时序 |
| FR-004 | 3.6 / 参考链接 — typing indicator |
| FR-005 | 3.3 — 关闭再打开需连接上一次会话并加载内容 |
| FR-006 | 3.5 — 支持新建会话并在新 session ID 中继续 |
| FR-008 | 3.4 — Markdown 格式解析渲染 |
| FR-009 | 2 — 通过 relay 使用 websocket 连接，不暴露 gateway 端口 |
| FR-010 | 4 — session 创建和重连 |
