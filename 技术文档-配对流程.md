# OpenClaw Gateway 配对流程技术文档

> 生成时间: 2026-04-26
> 状态: 问题排查中

---

## 一、核心概念

### 1.1 设备身份 (Device Identity)

Gateway 使用 Ed25519 非对称加密来验证设备身份：

| 组件 | 说明 |
|------|------|
| **publicKey** | Ed25519 公钥，32 字节原始数据 |
| **privateKey** | Ed25519 私钥，PKCS8 PEM 格式 |
| **deviceId** | 设备唯一标识 = SHA256(publicKey 原始 32 字节) |
| **deviceToken** | Gateway 颁发的配对凭证，用于已配对设备的认证 |

### 1.2 Gateway Token

Gateway 用于内部认证的共享密钥：

```
b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738
```

---

## 二、Gateway 存储的已配对设备信息

### 2.1 设备身份文件
路径: `~/.openclaw/identity/device.json`

```json
{
  "version": 1,
  "deviceId": "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4",
  "publicKeyPem": "-----BEGIN PUBLIC KEY-----\nMCowBQYDK2VwAyEAGLzuD6cdvyMe1uvyoSBBFkvUAorPkEk83X+vW/A+z9E=\n-----END PUBLIC KEY-----\n",
  "privateKeyPem": "-----BEGIN PRIVATE KEY-----\nMC4CAQAwBQYDK2VwBCIEIHGB9Jd7PbFgqSzgZbsMc0UN7JvrE+7AmswJIzqCoLGC\n-----END PRIVATE KEY-----\n",
  "createdAtMs": 1777187375818
}
```

### 2.2 设备认证信息
路径: `~/.openclaw/identity/device-auth.json`

```json
{
  "version": 1,
  "deviceId": "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4",
  "tokens": {
    "operator": {
      "token": "fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU",
      "role": "operator",
      "scopes": [
        "operator.admin",
        "operator.approvals",
        "operator.pairing",
        "operator.read",
        "operator.talk.secrets",
        "operator.write"
      ],
      "updatedAtMs": 1777214741137
    }
  }
}
```

### 2.3 关键发现

Gateway 已有配对设备：
- **deviceId**: `2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4`
- **operator token**: `fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU`
- **publicKeyPem**: `MCowBQYDK2VwAyEAGLzuD6cdvyMe1uvyoSBBFkvUAorPkEk83X+vW/A+z9E=`

---

## 三、WebSocket 连接流程

### 3.1 连接建立

1. Relay 连接到 `ws://127.0.0.1:18789`
2. Gateway 发送 `connect.challenge` 事件

```json
{
  "type": "event",
  "event": "connect.challenge",
  "payload": {
    "nonce": "4999fb05-ba30-476d-bee5-5fef767294ee",
    "ts": 1745714737323
  }
}
```

### 3.2 完整 V2 签名 payload 格式

```
v2|{deviceId}|{clientId}|{clientMode}|{role}|{scopes}|{signedAtMs}|{token}|{nonce}
```

**示例**:
```
v2|2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4|cli|cli|operator|operator.admin,operator.approvals,operator.pairing,operator.read,operator.talk.secrets,operator.write|1745714737000|b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738|4999fb05-ba30-476d-bee5-5fef767294ee
```

### 3.3 签名算法

- **算法**: Ed25519 (Node.js `crypto.sign(null, ...)`)
- **输入**: payload 字符串 (UTF-8), privateKey PEM
- **输出**: Base64 编码的签名

### 3.4 Connect 请求格式

```json
{
  "type": "req",
  "id": "relay-connect",
  "method": "connect",
  "params": {
    "minProtocol": 3,
    "maxProtocol": 3,
    "client": {
      "id": "cli",
      "version": "1.0.0",
      "platform": "linux",
      "mode": "cli"
    },
    "role": "operator",
    "scopes": [
      "operator.admin",
      "operator.read",
      "operator.write",
      "operator.approvals",
      "operator.pairing"
    ],
    "auth": {
      "token": "b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738"
    },
    "device": {
      "id": "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4",
      "publicKey": "{base64 编码的 32 字节公钥}",
      "signature": "{base64 编码的 Ed25519 签名}",
      "signedAt": 1745714737000,
      "nonce": "4999fb05-ba30-476d-bee5-5fef767294ee"
    }
  }
}
```

---

## 四、Gateway 签名验证流程 (源码级)

### 4.1 验证步骤 (来自 `message-handler.ts`)

```typescript
// 1. 派生 deviceId 并验证匹配
const derivedId = deriveDeviceIdFromPublicKey(device.publicKey);
if (!derivedId || derivedId !== device.id) {
  rejectDeviceAuthInvalid("device-id-mismatch");
  return;
}

// 2. 验证时间戳 (5 分钟内)
const signedAt = device.signedAt;
if (Math.abs(Date.now() - signedAt) > DEVICE_SIGNATURE_SKEW_MS) {
  rejectDeviceAuthInvalid("device-signature-stale");
  return;
}

// 3. 验证 nonce 与 challenge 一致
const providedNonce = device.nonce;
if (providedNonce !== connectNonce) {
  rejectDeviceAuthInvalid("device-nonce-mismatch");
  return;
}

// 4. 验证签名 (尝试 V3 和 V2)
const payloadVersion = resolveDeviceSignaturePayloadVersion({
  device, connectParams, role, scopes, signedAtMs, nonce
});
if (!payloadVersion) {
  rejectDeviceAuthInvalid("device-signature");
  return;
}
```

### 4.2 deviceId 派生算法

```typescript
function deriveDeviceIdFromPublicKey(publicKeyBase64: string): string {
  // 如果是 PEM 格式，先提取原始 32 字节
  const rawKey = publicKey.includes("BEGIN")
    ? extractRawFromPem(publicKey)  // 去掉 SPKI header
    : base64Decode(publicKey);

  // DeviceId = SHA256(raw 32 bytes)
  return sha256(rawKey).toString('hex');
}
```

### 4.3 V2 Payload 构建 (来自 `device-auth.ts`)

```typescript
export function buildDeviceAuthPayload(params: DeviceAuthPayloadParams): string {
  const scopes = params.scopes.join(",");
  const token = params.token ?? "";
  return [
    "v2",
    params.deviceId,
    params.clientId,
    params.clientMode,
    params.role,
    scopes,
    String(params.signedAtMs),
    token,
    params.nonce,
  ].join("|");
}
```

### 4.4 签名验证 (来自 `device-identity.ts`)

```typescript
export function verifyDeviceSignature(
  publicKey: string,
  payload: string,
  signatureBase64Url: string,
): boolean {
  const key = publicKey.includes("BEGIN")
    ? crypto.createPublicKey(publicKey)  // PEM 格式
    : crypto.createPublicKey({
        key: Buffer.concat([ED25519_SPKI_PREFIX, base64UrlDecode(publicKey)]),
        type: "spki",
        format: "der",
      });

  const sig = base64UrlDecode(signatureBase64Url);
  return crypto.verify(null, Buffer.from(payload, "utf8"), key, sig);
}
```

---

## 五、node.pair.request 配对请求流程

### 5.1 何时调用

当设备首次连接，Gateway 返回 `NOT_PAIRED` 错误时调用。

### 5.2 请求格式

```json
{
  "type": "req",
  "id": "pair-request",
  "method": "node.pair.request",
  "params": {
    "nodeId": "设备公钥的 SHA256",
    "displayName": "OpenClaw Relay",
    "platform": "linux",
    "version": "1.0.0"
  }
}
```

**注意**: `node.pair.request` 的 params 不包含 `role` 和 `scopes`（schema 有 `additionalProperties: false`）

### 5.3 Gateway 处理

1. 验证 `nodeId` 格式
2. 创建待审批配对请求
3. 广播 `node.pair.requested` 事件
4. 返回响应 `{ ok: true, status: "pending", requestId: "..." }`

### 5.4 管理员审批

```bash
openclaw nodes list                    # 查看待审批请求
openclaw nodes approve <requestId>     # 审批
```

---

## 六、当前代码实现

### 6.1 pairing.ts - 配对流程

**文件**: `relay-server/src/server/pairing.ts`

#### 6.1.1 加载设备密钥

```typescript
export function loadOrCreateDeviceKey(): DeviceKey {
  // 1. 优先从本地保存的密钥加载
  if (existsSync(DEVICE_KEY_PATH)) {
    return JSON.parse(readFileSync(DEVICE_KEY_PATH, 'utf-8'));
  }

  // 2. 从 Gateway identity 文件加载 (如果已配对)
  const gatewayDeviceIdentityPath = join(homedir(), '.openclaw', 'identity', 'device.json');
  if (existsSync(gatewayDeviceIdentityPath)) {
    const identity = JSON.parse(readFileSync(gatewayDeviceIdentityPath, 'utf-8'));
    const publicKey = createPublicKey(identity.publicKeyPem);
    const rawKey = publicKey.export({ type: 'spki', format: 'der' }).slice(-32);
    const deviceId = createHash('sha256').update(rawKey).digest('hex');
    return {
      deviceId,
      publicKey: rawKey.toString('base64'),  // 32 字节 base64
      privateKey: identity.privateKeyPem,
    };
  }

  // 3. 生成新密钥对
  return generateDeviceKey();
}
```

#### 6.1.2 加载已保存 Token

```typescript
export function loadSavedToken(): string | null {
  // 1. 优先从本地文件加载
  if (existsSync(TOKEN_PATH)) {
    return readFileSync(TOKEN_PATH, 'utf-8').trim();
  }

  // 2. 从 Gateway device-auth.json 加载
  const gatewayDeviceAuthPath = join(homedir(), '.openclaw', 'identity', 'device-auth.json');
  if (existsSync(gatewayDeviceAuthPath)) {
    const auth = JSON.parse(readFileSync(gatewayDeviceAuthPath, 'utf-8'));
    return auth.tokens?.operator?.token;
  }

  return null;
}
```

#### 6.1.3 V2 签名 Payload 构建

```typescript
const GATEWAY_TOKEN = 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738';
const scopes = ['operator.admin', 'operator.read', 'operator.write', 'operator.approvals', 'operator.pairing'].join(',');
const payload = `v2|${deviceKey.deviceId}|cli|cli|operator|${scopes}|${signedAtMs}|${GATEWAY_TOKEN}|${challengePayload.nonce}`;
const signature = signData(payload, deviceKey.privateKey);
```

#### 6.1.4 签名函数

```typescript
export function signData(data: string, privateKey: string): string {
  const signature = cryptoSign(null, Buffer.from(data), privateKey);
  return signature.toString('base64');
}
```

### 6.2 websocket.ts - Gateway 连接

**文件**: `relay-server/src/server/websocket.ts`

```typescript
if (frame.type === 'event' && frame.event === 'connect.challenge') {
  const challengePayload = frame.payload as { nonce: string; ts: number };

  // 加载设备密钥
  const deviceKey = loadOrCreateDeviceKey();
  const signedAt = Date.now();

  // V2 payload
  const GATEWAY_TOKEN = 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738';
  const scopes = ['operator.admin', 'operator.read', 'operator.write', 'operator.approvals', 'operator.pairing'].join(',');
  const payload = `v2|${deviceKey.deviceId}|cli|cli|operator|${scopes}|${signedAt}|${GATEWAY_TOKEN}|${challengePayload.nonce}`;
  const signature = signData(payload, deviceKey.privateKey);

  const connectParams: ConnectParams = {
    role: 'operator',
    scopes: [...],
    device: {
      id: deviceKey.deviceId,
      publicKey: deviceKey.publicKey,
      signature: signature,
      signedAt: signedAt,
      nonce: challengePayload.nonce,
    },
    auth: {
      deviceToken: this.config.pairing.deviceToken,
    },
  };

  ws.send(JSON.stringify({
    type: 'req',
    id: 'relay-connect',
    method: 'connect',
    params: connectParams,
  }));
}
```

---

## 七、问题排查

### 7.1 当前错误

```
DEVICE_AUTH_SIGNATURE_INVALID
reason: "device-signature"
```

### 7.2 可能原因

| # | 可能性 | 说明 |
|---|--------|------|
| 1 | **设备密钥与 Gateway 记录不匹配** | 我们使用的密钥对与 Gateway 存储的公钥不一致 |
| 2 | **deviceId 计算错误** | SHA256(publicKey) != deviceId |
| 3 | **Nonce 不一致** | 签名时用的 nonce 与 challenge 中的不一致 |
| 4 | **Payload 格式错误** | V2 payload 某部分格式不对 |
| 5 | **签名算法问题** | Node.js crypto.sign 与 Gateway 期望的不兼容 |

### 7.3 验证清单

```bash
# 1. 检查 Gateway 存储的 deviceId
cat ~/.openclaw/identity/device.json | jq .deviceId

# 2. 检查 Gateway 存储的公钥
cat ~/.openclaw/identity/device.json | jq .publicKeyPem

# 3. 用 Gateway 公钥验证签名 (本地测试)
node -e "
const crypto = require('crypto');
const publicKeyPem = \`-----BEGIN PUBLIC KEY-----
MCowBQYDK2VwAyEAGLzuD6cdvyMe1uvyoSBBFkvUAorPkEk83X+vW/A+z9E=
-----END PUBLIC KEY-----\`;

const payload = 'v2|2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4|cli|cli|operator|operator.admin,operator.approvals,operator.pairing,operator.read,operator.talk.secrets,operator.write|1745714737000|b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738|4999fb05-ba30-476d-bee5-5fef767294ee';

const signature = '<从日志中获取的签名>';

const key = crypto.createPublicKey(publicKeyPem);
const sig = Buffer.from(signature, 'base64');
const ok = crypto.verify(null, Buffer.from(payload, 'utf8'), key, sig);
console.log('Signature valid:', ok);
"
```

---

## 八、Relay 端日志解读

```
[INFO] Loaded device token from Gateway device-auth.json
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"
  -> 正确加载了 Gateway 已配对设备的 token

[INFO] Loaded device key from Gateway identity (already paired)
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"
  -> 正确从 Gateway identity 文件加载了密钥

[INFO] Received connect.challenge
  nonce: "4999fb05-ba30-476d-bee5-5fef767294ee"
  -> 收到 Gateway challenge

[ERROR] Gateway rejected connect
  error.code: "INVALID_REQUEST"
  error.details.code: "DEVICE_AUTH_SIGNATURE_INVALID"
  error.details.reason: "device-signature"
  -> 签名验证失败
```

---

## 九、关键数据对比表

| 项目 | Gateway 存储值 | Relay 当前值 | 是否匹配 |
|------|---------------|-------------|---------|
| deviceId | `2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4` | `?` | 待确认 |
| publicKey | `MCowBQYDK2VwAyEAGLzuD6cdvyMe1uvyoSBBFkvUAorPkEk83X+vW/A+z9E=` | `?` | 待确认 |
| operator token | `fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU` | `fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU` | ✓ |

---

## 十、修复方向

### 10.1 当前最可能的问题

**设备密钥加载逻辑可能没有正确从 Gateway identity 文件读取**

检查点：
1. `loadOrCreateDeviceKey()` 是否真的从 `~/.openclaw/identity/device.json` 读取？
2. 读取后的 `deviceId` 计算是否正确？
3. `publicKey` 格式是否正确（应该是 32 字节 raw base64，不是 PEM）？

### 10.2 建议的验证步骤

1. **在 relay 启动时打印完整的 deviceKey 对象**，确认：
   - `deviceId` 是否等于 `2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4`
   - `publicKey` 是否是 32 字节的 base64
   - `privateKey` 是否可以用于签名

2. **手动计算 deviceId**：
   ```typescript
   const publicKeyPem = "-----BEGIN PUBLIC KEY-----\nMCowBQYDK2VwAyEAGLzuD6cdvyMe1uvyoSBBFkvUAorPkEk83X+vW/A+z9E=\n-----END PUBLIC KEY-----\n";
   const key = crypto.createPublicKey(publicKeyPem);
   const spki = key.export({ type: 'spki', format: 'der' }); // 44 字节 (SPKI header + 32)
   const raw = spki.slice(-32); // 最后 32 字节
   const deviceId = crypto.createHash('sha256').update(raw).digest('hex');
   console.log(deviceId); // 应该是 2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4
   ```

3. **手动验证签名**：
   用 Gateway 的公钥验证我们自己签名的 payload

---

## 十一、相关文件路径

| 文件 | 路径 | 说明 |
|------|------|------|
| Gateway identity | `~/.openclaw/identity/device.json` | 设备公钥/私钥 |
| Gateway auth | `~/.openclaw/identity/device-auth.json` | 设备 token |
| Relay device key | `~/.openclaw-relay/device-key.json` | Relay 自己的密钥 |
| Relay token | `~/.openclaw-relay/device-token` | Relay 保存的 token |

---

## 十二、Relay 当前完整代码

### 12.1 pairing.ts - 配对流程核心

**文件**: `relay-server/src/server/pairing.ts`

```typescript
/**
 * OpenClaw Gateway WebSocket Pairing Flow
 *
 * 流程:
 * 1. 生成设备密钥对 (Ed25519)
 * 2. 连接 Gateway WebSocket，发送 connect 请求 (role: "node", 携带设备公钥)
 * 3. Gateway 返回 NOT_PAIRED (DEVICE_IDENTITY_REQUIRED)
 * 4. 调用 node.pair.request 方法发起配对请求
 * 5. Gateway 发送 node.pair.requested 事件 (表示待审批)
 * 6. 管理员运行 openclaw nodes approve <requestId> 审批
 * 7. Gateway 发送 node.pair.resolved 事件 (包含 token)
 * 8. 保存 token，用于后续连接
 */

import { sign as cryptoSign, generateKeyPairSync, createHash, createPublicKey } from 'crypto';
import { readFileSync, writeFileSync, existsSync, mkdirSync } from 'fs';
import { homedir } from 'os';
import { join } from 'path';
import WebSocket from 'ws';
import { logger } from '../logger.js';

const PAIRING_TIMEOUT_MS = 300000; // 5 minutes
const DEVICE_KEY_PATH = join(homedir(), '.openclaw-relay', 'device-key.json');

// ============================================================================
// 类型定义
// ============================================================================

export interface PairingResult {
  deviceToken: string;
  deviceId: string;
  pendingApproval?: boolean;
  pairingToken?: string | null;
}

interface DeviceKey {
  deviceId: string;
  publicKey: string; // 32字节原始公钥的 base64 编码
  privateKey: string; // PKCS8 PEM 格式私钥
}

// ============================================================================
// 密钥生成与加载
// ============================================================================

/**
 * Generate a new Ed25519 key pair and compute deviceId (SHA256 of raw 32-byte public key)
 */
export function generateDeviceKey(): DeviceKey {
  const { publicKey, privateKey } = generateKeyPairSync('ed25519');

  // DeviceId = SHA256 of raw 32-byte public key
  const rawKey = (publicKey as any).export({ type: 'spki', format: 'der' }).slice(-32);
  const deviceId = createHash('sha256').update(rawKey).digest('hex');

  // Store raw 32-byte base64 for public key (Gateway derives deviceId from this)
  const publicKeyRaw = rawKey.toString('base64');
  const privateKeyPem = (privateKey as any).export({ type: 'pkcs8', format: 'pem' }) as string;

  return {
    deviceId,
    publicKey: publicKeyRaw,
    privateKey: privateKeyPem,
  };
}

/**
 * 生成或加载设备密钥对
 *
 * 优先级:
 * 1. ~/.openclaw-relay/device-key.json (本地保存的密钥)
 * 2. ~/.openclaw/identity/device.json (Gateway 已配对的设备密钥)
 * 3. 生成新的密钥对
 */
export function loadOrCreateDeviceKey(): DeviceKey {
  // 1. 尝试从本地保存的密钥加载
  try {
    if (existsSync(DEVICE_KEY_PATH)) {
      const content = readFileSync(DEVICE_KEY_PATH, 'utf-8');
      const key = JSON.parse(content) as DeviceKey;
      logger.info({ deviceId: key.deviceId }, 'Loaded existing device key from file');
      return key;
    }
  } catch (err) {
    logger.warn({ err }, 'Failed to load device key from file');
  }

  // 2. 尝试从 Gateway 的 device identity 文件加载 (用于已配对设备)
  const gatewayDeviceIdentityPath = join(homedir(), '.openclaw', 'identity', 'device.json');
  try {
    if (existsSync(gatewayDeviceIdentityPath)) {
      const content = readFileSync(gatewayDeviceIdentityPath, 'utf-8');
      const identity = JSON.parse(content);
      if (identity.privateKeyPem && identity.publicKeyPem) {
        // Compute deviceId from the public key (same as Gateway does)
        const publicKey = createPublicKey(identity.publicKeyPem);
        const rawKey = publicKey.export({ type: 'spki', format: 'der' }).slice(-32);
        const deviceId = createHash('sha256').update(rawKey).digest('hex');
        const deviceKey: DeviceKey = {
          deviceId,
          publicKey: rawKey.toString('base64'),  // 32字节原始公钥
          privateKey: identity.privateKeyPem,     // PKCS8 PEM 格式
        };
        logger.info({ deviceId }, 'Loaded device key from Gateway identity (already paired)');
        // 保存到本地以便下次使用
        try {
          const dir = join(homedir(), '.openclaw-relay');
          mkdirSync(dir, { recursive: true });
          writeFileSync(DEVICE_KEY_PATH, JSON.stringify(deviceKey, null, 2));
        } catch {}
        return deviceKey;
      }
    }
  } catch (err) {
    logger.warn({ err }, 'Failed to load device key from Gateway identity');
  }

  // 3. 生成新密钥对
  logger.info('Generating new Ed25519 device key...');
  const newDevice = generateDeviceKey();
  logger.info({ deviceId: newDevice.deviceId }, 'Generated new device key');

  // 保存以便下次使用
  try {
    const dir = join(homedir(), '.openclaw-relay');
    mkdirSync(dir, { recursive: true });
    writeFileSync(DEVICE_KEY_PATH, JSON.stringify(newDevice, null, 2));
    logger.info({ deviceId: newDevice.deviceId }, 'Saved new device key');
  } catch (err) {
    logger.error({ err }, 'Failed to save device key');
  }

  return newDevice;
}

/**
 * 对数据签名 (使用 Ed25519)
 */
export function signData(data: string, privateKey: string): string {
  const signature = cryptoSign(null, Buffer.from(data), privateKey);
  return signature.toString('base64');
}

// ============================================================================
// Token 加载与保存
// ============================================================================

const TOKEN_PATH = join(homedir(), '.openclaw-relay', 'device-token');

function saveToken(token: string): void {
  try {
    const dir = join(homedir(), '.openclaw-relay');
    require('fs').mkdirSync(dir, { recursive: true });
    require('fs').writeFileSync(TOKEN_PATH, token, { mode: 0o600 });
    logger.info('Device token saved');
  } catch (err) {
    logger.error({ err }, 'Failed to save token');
  }
}

/**
 * 加载已保存的设备 token
 *
 * 优先级:
 * 1. ~/.openclaw-relay/device-token (本地保存的 token)
 * 2. ~/.openclaw/identity/device-auth.json (Gateway 的 token)
 */
export function loadSavedToken(): string | null {
  // 1. 尝试从本地文件加载
  try {
    if (existsSync(TOKEN_PATH)) {
      return readFileSync(TOKEN_PATH, 'utf-8').trim();
    }
  } catch (err) {
    // ignore
  }

  // 2. 尝试从 Gateway 的 device-auth.json 加载 (用于已配对设备)
  const gatewayDeviceAuthPath = join(homedir(), '.openclaw', 'identity', 'device-auth.json');
  try {
    if (existsSync(gatewayDeviceAuthPath)) {
      const content = readFileSync(gatewayDeviceAuthPath, 'utf-8');
      const auth = JSON.parse(content);
      if (auth.tokens?.operator?.token) {
        logger.info({ deviceId: auth.deviceId }, 'Loaded device token from Gateway device-auth.json');
        return auth.tokens.operator.token;
      }
    }
  } catch (err) {
    // ignore
  }

  return null;
}

// ============================================================================
// 配对流程 (用于首次配对)
// ============================================================================

/**
 * 通过 WebSocket 连接 Gateway 并执行配对流程
 *
 * 流程:
 * 1. 连接 without device identity → 收到 NOT_PAIRED
 * 2. 发送 node.pair.request → Gateway 关闭连接 (预期行为)
 * 3. 管理员审批
 * 4. 重新连接 with device identity → 成功
 */
export async function performPairingWebSocket(
  gatewayHost: string,
  gatewayPort: number,
  onStatusChange?: (status: string) => void
): Promise<PairingResult> {
  const deviceKey = loadOrCreateDeviceKey();
  const wsUrl = `ws://${gatewayHost}:${gatewayPort}`;

  logger.info({ wsUrl, deviceId: deviceKey.deviceId }, 'Starting WebSocket pairing flow');

  return new Promise((resolve, reject) => {
    let ws: WebSocket | null = null;
    let resolved = false;
    let waitingForPairing = false;
    let pairingAttempt = 0;
    let pairingToken: string | null = null;

    const cleanup = () => {
      if (ws) {
        ws.removeAllListeners();
        if (ws.readyState === WebSocket.OPEN) {
          ws.close();
        }
        ws = null;
      }
    };

    const timeout = setTimeout(() => {
      if (!resolved) {
        resolved = true;
        cleanup();
        reject(new Error('Pairing timeout (5 minutes)'));
      }
    }, PAIRING_TIMEOUT_MS);

    const fail = (err: Error) => {
      if (!resolved) {
        resolved = true;
        clearTimeout(timeout);
        cleanup();
        reject(err);
      }
    };

    try {
      ws = new WebSocket(wsUrl);

      ws.on('open', () => {
        logger.info('WebSocket connected, waiting for challenge...');
      });

      ws.on('message', (data) => {
        try {
          const frame = JSON.parse(data.toString());

          // ================================================================
          // 收到 connect.challenge
          // ================================================================
          if (frame.type === 'event' && frame.event === 'connect.challenge') {
            const challengePayload = frame.payload as { nonce: string; ts: number };
            logger.info({ nonce: challengePayload.nonce, pairingAttempt }, 'Received connect.challenge');

            if (pairingAttempt === 0) {
              // 第一次尝试: 不带设备身份发送 connect
              logger.info('Attempt 0: Sending connect without device identity...');
              ws!.send(JSON.stringify({
                type: 'req',
                id: 'relay-connect',
                method: 'connect',
                params: {
                  minProtocol: 3,
                  maxProtocol: 3,
                  client: {
                    id: 'cli',
                    version: '1.0.0',
                    platform: 'linux',
                    mode: 'cli',
                  },
                  role: 'node',
                  scopes: [],
                  auth: {
                    token: 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738',
                  },
                },
              }));
              pairingAttempt = 1;
            } else {
              // 后续尝试: 带设备身份发送 connect
              logger.info(`Attempt ${pairingAttempt}: Sending connect with device identity...`);
              const GATEWAY_TOKEN = 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738';
              const scopes = ['operator.admin', 'operator.read', 'operator.write', 'operator.approvals', 'operator.pairing'].join(',');
              const signedAtMs = Date.now();

              // V2 payload 格式: v2|deviceId|clientId|clientMode|role|scopes|signedAtMs|token|nonce
              const payload = `v2|${deviceKey.deviceId}|cli|cli|operator|${scopes}|${signedAtMs}|${GATEWAY_TOKEN}|${challengePayload.nonce}`;
              logger.info({ payload, publicKey: deviceKey.publicKey }, 'Signature payload for v2');
              const signature = signData(payload, deviceKey.privateKey);

              ws!.send(JSON.stringify({
                type: 'req',
                id: 'relay-connect',
                method: 'connect',
                params: {
                  minProtocol: 3,
                  maxProtocol: 3,
                  client: {
                    id: 'cli',
                    version: '1.0.0',
                    platform: 'linux',
                    mode: 'cli',
                  },
                  role: 'node',
                  scopes: ['operator.admin', 'operator.read', 'operator.write', 'operator.approvals', 'operator.pairing'],
                  auth: {
                    token: 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738',
                  },
                  device: {
                    id: deviceKey.deviceId,
                    publicKey: deviceKey.publicKey,
                    signature: signature,
                    signedAt: signedAtMs,
                    nonce: challengePayload.nonce,
                  },
                },
              }));
            }
          }
          // ================================================================
          // 收到 connect 响应
          // ================================================================
          else if (frame.type === 'res' && frame.id === 'relay-connect') {
            if (!frame.ok) {
              const errorCode = frame.error?.code;
              const errorDetails = frame.error?.details;

              if (errorCode === 'NOT_PAIRED' && errorDetails?.code === 'DEVICE_IDENTITY_REQUIRED') {
                // 发送配对请求
                logger.info('Sending node.pair.request...');
                waitingForPairing = true;

                ws!.send(JSON.stringify({
                  type: 'req',
                  id: 'pair-request',
                  method: 'node.pair.request',
                  params: {
                    nodeId: deviceKey.deviceId,
                    displayName: 'OpenClaw Relay',
                    platform: 'linux',
                    version: '1.0.0',
                  },
                }));
              } else {
                logger.error({ errorCode, errorDetails }, 'Connect rejected');
                fail(new Error(`Connect rejected: ${frame.error?.message}`));
              }
            } else {
              // 连接成功 (已配对)
              logger.info({ connId: frame.payload?.server?.connId }, 'Already paired, connection successful');
              resolved = true;
              clearTimeout(timeout);

              const auth = frame.payload?.auth;
              if (auth?.deviceToken) {
                saveToken(auth.deviceToken);
                resolve({ deviceToken: auth.deviceToken, deviceId: deviceKey.deviceId });
              } else {
                fail(new Error('No device token in successful connection'));
              }
            }
          }
          // ================================================================
          // 收到 node.pair.request 响应
          // ================================================================
          else if (frame.type === 'res' && frame.id === 'pair-request') {
            logger.info({ frame }, 'node.pair.request response');
            if (frame.ok) {
              const payload = frame.payload as { requestId?: string; status?: string; token?: string };
              pairingToken = payload.requestId || null;
              logger.info({ pairingToken, hasToken: !!payload.token, payload }, 'Pairing request submitted, awaiting admin approval...');

              if (payload.token) {
                // 配对被自动批准
                logger.info('Pairing auto-approved with token!');
                saveToken(payload.token);
                resolved = true;
                clearTimeout(timeout);
                resolve({ deviceToken: payload.token, deviceId: deviceKey.deviceId });
              } else {
                // 需要管理员审批 - WebSocket 会关闭，所以直接返回待审批状态
                onStatusChange?.('awaiting_approval');
                resolved = true;
                clearTimeout(timeout);
                resolve({
                  deviceToken: '',
                  deviceId: deviceKey.deviceId,
                  pendingApproval: true,
                  pairingToken: pairingToken,
                } as any);
              }
            } else {
              logger.error({ error: frame.error }, 'Pairing request failed');
              fail(new Error(`Pairing request failed: ${frame.error?.message}`));
            }
          }
          // ================================================================
          // 收到 node.pair.requested 事件
          // ================================================================
          else if (frame.type === 'event' && frame.event === 'node.pair.requested') {
            const payload = frame.payload as { requestId?: string };
            logger.info({ requestId: payload.requestId }, 'Gateway received pairing request, waiting for approval...');
            onStatusChange?.('awaiting_approval');
          }
          // ================================================================
          // 收到 node.pair.resolved 事件 (审批结果)
          // ================================================================
          else if (frame.type === 'event' && frame.event === 'node.pair.resolved') {
            const payload = frame.payload as {
              requestId?: string;
              status?: 'approved' | 'rejected' | 'expired';
              token?: string;
              error?: string;
            };

            if (payload.status === 'approved' && payload.token) {
              logger.info('Pairing approved by admin!');
              onStatusChange?.('approved');
              saveToken(payload.token);
              resolved = true;
              clearTimeout(timeout);
              resolve({ deviceToken: payload.token, deviceId: deviceKey.deviceId });
            } else if (payload.status === 'rejected') {
              fail(new Error('Pairing was rejected by admin'));
            } else if (payload.status === 'expired') {
              fail(new Error('Pairing request expired'));
            } else {
              logger.warn({ payload }, 'Unknown node.pair.resolved payload');
            }
          }
          // ================================================================
          // 其他事件
          // ================================================================
          else if (frame.type === 'event') {
            logger.debug({ event: frame.event, payload: frame.payload }, 'Received event');
          }
          else if (frame.type === 'res') {
            logger.info({ frameId: frame.id, ok: frame.ok, payload: frame.payload, error: frame.error }, 'Unhandled response');
          }
        } catch (err) {
          logger.error({ err }, 'Error processing message');
        }
      });

      ws.on('error', (err) => {
        logger.error({ err }, 'WebSocket error');
        fail(err);
      });

      ws.on('close', () => {
        logger.info('WebSocket closed, waitingForPairing=' + waitingForPairing);
        if (!resolved && !waitingForPairing) {
          fail(new Error('WebSocket closed unexpectedly'));
        }
        // node.pair.request 后 Gateway 会关闭连接，管理员审批后重新连接
      });

    } catch (err) {
      fail(err as Error);
    }
  });
}
```

---

### 12.2 websocket.ts - Gateway 连接处理

**文件**: `relay-server/src/server/websocket.ts`

```typescript
import { WebSocketServer, WebSocket } from 'ws';
import { randomUUID } from 'crypto';
import { logger } from '../logger.js';
import { MessageRelay } from './relay.js';
import { loadOrCreateDeviceKey, signData } from './pairing.js';
import type {
  RelayConfig,
  GatewayFrame,
  ConnectParams,
  ClientId,
  ClientMode,
  HelloOkPayload,
} from '@openclaw/shared';

interface ExtConnection {
  id: string;
  ws: WebSocket;
  remoteIp: string;
  connectedAt: number;
  authenticated: boolean;
  connId?: string;
}

interface GwConnection {
  ws: WebSocket | null;
  connectedAt: number | null;
  authenticated: boolean;
  connId?: string;
}

export class RelayWebSocketServer {
  private extConnections = new Map<string, ExtConnection>();
  private gwConn: GwConnection = { ws: null, connectedAt: null, authenticated: false };
  private relay = new MessageRelay();
  private config: RelayConfig;
  private wss: WebSocketServer | null = null;
  private extHeartbeatTimer: ReturnType<typeof setInterval> | null = null;
  private gwHeartbeatTimer: ReturnType<typeof setInterval> | null = null;

  constructor(config: RelayConfig) {
    this.config = config;
  }

  async start() {
    this.wss = new WebSocketServer({ port: this.config.port, host: this.config.host });

    this.wss.on('connection', (ws, req) => {
      const extId = randomUUID();
      const remoteIp = req.socket.remoteAddress || 'unknown';
      this.extConnections.set(extId, { id: extId, ws, remoteIp, connectedAt: Date.now(), authenticated: false });
      this.relay.registerExt(extId);
      logger.info({ extId, remoteIp }, 'Extension connected');

      ws.on('message', (data) => this.handleExtMessage(extId, data));
      ws.on('close', () => this.handleExtClose(extId));
      ws.on('error', (err) => logger.error({ extId, err }, 'Extension WS error'));
      ws.on('pong', () => {
        logger.debug({ extId }, 'Extension pong received');
      });
    });

    await this.connectToGatewayWithHandshake();
    this.startHeartbeat();

    logger.info({ port: this.config.port }, 'Relay WebSocket server started');
  }

  /**
   * 与 Gateway 建立 WebSocket 连接并进行握手
   *
   * 流程:
   * 1. 连接到 Gateway WebSocket
   * 2. 收到 connect.challenge (包含 nonce)
   * 3. 使用设备密钥签名 V2 payload
   * 4. 发送 connect 请求 (带 device 身份)
   * 5. 收到 hello-ok 表示连接成功
   */
  private async connectToGatewayWithHandshake(): Promise<void> {
    const { host, port } = this.config.gateway;
    const url = `ws://${host}:${port}`;

    return new Promise<void>((resolve, reject) => {
      const ws = new WebSocket(url);
      let resolved = false;

      const cleanup = () => {
        ws.removeAllListeners();
      };

      ws.on('open', async () => {
        logger.info({ host, port }, 'WS connected to Gateway, waiting for challenge...');
      });

      ws.on('message', (data) => {
        if (resolved) return;

        try {
          const frame = JSON.parse(data.toString()) as GatewayFrame;

          // ================================================================
          // 收到 connect.challenge
          // ================================================================
          if (frame.type === 'event' && frame.event === 'connect.challenge') {
            const challengePayload = frame.payload as { nonce: string; ts: number };
            logger.info({ nonce: challengePayload.nonce }, 'Received connect.challenge, sending connect request...');

            // 加载设备密钥
            const deviceKey = loadOrCreateDeviceKey();
            const signedAt = Date.now();

            // V2 payload 格式: v2|deviceId|clientId|clientMode|role|scopes|signedAtMs|token|nonce
            const GATEWAY_TOKEN = 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738';
            const scopes = ['operator.admin', 'operator.read', 'operator.write', 'operator.approvals', 'operator.pairing'].join(',');
            const payload = `v2|${deviceKey.deviceId}|cli|cli|operator|${scopes}|${signedAt}|${GATEWAY_TOKEN}|${challengePayload.nonce}`;
            const signature = signData(payload, deviceKey.privateKey);

            const connectParams: ConnectParams = {
              minProtocol: 3,
              maxProtocol: 3,
              client: {
                id: 'cli' as ClientId,
                version: '1.0.0',
                platform: 'linux',
                mode: 'cli' as ClientMode,
              },
              role: 'operator',
              scopes: ['operator.admin', 'operator.read', 'operator.write', 'operator.approvals', 'operator.pairing'],
              device: {
                id: deviceKey.deviceId,
                publicKey: deviceKey.publicKey,
                signature: signature,
                signedAt: signedAt,
                nonce: challengePayload.nonce,  // 使用 challenge 中的 nonce
              },
            };

            // 如果有 device token，加上认证信息
            if (this.config.pairing.deviceToken) {
              connectParams.auth = {
                deviceToken: this.config.pairing.deviceToken,
              };
            }

            ws.send(JSON.stringify({
              type: 'req',
              id: 'relay-connect',
              method: 'connect',
              params: connectParams,
            }));

          }
          // ================================================================
          // 收到 connect 响应
          // ================================================================
          else if (frame.type === 'res' && frame.id === 'relay-connect') {
            cleanup();

            if (!frame.ok) {
              logger.error({ error: frame.error }, 'Gateway rejected connect');
              ws.close();
              reject(new Error(`Connect rejected: ${frame.error?.message}`));
              return;
            }

            const helloOk = frame.payload as HelloOkPayload;
            logger.info({ connId: helloOk.server.connId, methods: helloOk.features.methods.length }, 'Received hello-ok, Gateway handshake complete');

            this.gwConn = {
              ws,
              connectedAt: Date.now(),
              authenticated: true,
              connId: helloOk.server.connId,
            };

            ws.on('message', (d) => this.handleGwMessage(d));
            ws.on('close', () => this.handleGwClose());
            ws.on('error', (err) => logger.error({ err }, 'Gateway WS error'));
            ws.on('pong', () => logger.debug('Gateway pong received'));

            resolved = true;
            resolve();

          } else {
            logger.warn({ frame }, 'Unexpected frame during handshake');
          }
        } catch (err) {
          logger.error({ err }, 'Failed to process handshake message');
          if (!resolved) {
            ws.close();
            reject(err);
          }
        }
      });

      ws.on('error', (err) => {
        logger.error({ err }, 'Gateway WS error during handshake');
        if (!resolved) reject(err);
      });

      ws.on('close', () => {
        if (!resolved) {
          resolved = true;
          reject(new Error('Gateway closed during handshake'));
        }
      });

      setTimeout(() => {
        if (!resolved) {
          cleanup();
          ws.close();
          reject(new Error('Gateway handshake timeout'));
        }
      }, 15000);
    });
  }

  // ... 其他方法省略 ...
}
```

---

### 12.3 main.ts - 程序入口

**文件**: `relay-server/src/main.ts`

```typescript
import { loadConfig } from './config.js';
import { logger } from './logger.js';
import { createHttpServer } from './server/http.js';
import { RelayWebSocketServer } from './server/websocket.js';
import { performPairingWebSocket, loadSavedToken } from './server/pairing.js';

async function main() {
  const config = loadConfig();

  // 检查是否有设备 token (优先级: 环境变量 > 已保存的 token > 配置文件)
  const savedToken = loadSavedToken();
  const envToken = process.env.DEVICE_TOKEN;
  const tokenToUse = envToken || savedToken || config.pairing.deviceToken;

  // ================================================================
  // 没有 token，需要配对
  // ================================================================
  if (!tokenToUse) {
    logger.info('No device token found, starting WebSocket pairing flow...');
    console.log('\n');
    console.log('╔════════════════════════════════════════════════════════════╗');
    console.log('║       OPENCLAW RELAY - GATEWAY PAIRING REQUIRED           ║');
    console.log('╠════════════════════════════════════════════════════════════╣');
    console.log('║  The relay needs to be paired with the Gateway.           ║');
    console.log('║                                                        ║');
    console.log('║  After the relay starts, you must approve it:           ║');
    console.log('║    1. On the gateway server, run:                        ║');
    console.log('║       openclaw nodes list                                ║');
    console.log('║    2. Find the pending pairing request                  ║');
    console.log('║    3. Run: openclaw nodes approve <requestId>            ║');
    console.log('║                                                        ║');
    console.log('║  The relay will auto-retry until pairing is approved.    ║');
    console.log('╚════════════════════════════════════════════════════════════╝');
    console.log('\n');

    // 配对重试循环
    while (true) {
      try {
        const result = await performPairingWebSocket(
          config.gateway.host,
          config.gateway.port,
          (status) => {
            logger.info({ status }, 'Pairing status update');
          }
        );

        if (result.pendingApproval) {
          // 配对请求已提交，等待管理员审批
          console.log('\n');
          console.log('╔════════════════════════════════════════════════════════════╗');
          console.log('║  PAIRING REQUEST SUBMITTED - AWAITING APPROVAL            ║');
          console.log('╠════════════════════════════════════════════════════════════╣');
          console.log('║  Please approve the pairing request on the Gateway:       ║');
          console.log('║    openclaw nodes list                                    ║');
          console.log('║    openclaw nodes approve ' + (result.pairingToken || '<requestId>').padEnd(28) + '║');
          console.log('║                                                        ║');
          console.log('║  The relay will retry automatically after approval...     ║');
          console.log('╚════════════════════════════════════════════════════════════╝');
          console.log('\n');

          // 等待后重试
          await new Promise(resolve => setTimeout(resolve, 5000));
          continue;
        }

        // 成功 - 获得 token
        logger.info({ deviceId: result.deviceId }, 'Pairing successful!');
        config.pairing.deviceToken = result.deviceToken;
        break;
      } catch (err) {
        logger.error({ err }, 'Pairing failed');
        process.exit(1);
      }
    }
  }
  // ================================================================
  // 有 token，直接使用
  // ================================================================
  else {
    if (envToken) {
      logger.info('Using device token from DEVICE_TOKEN env var');
    } else if (savedToken) {
      logger.info('Using saved device token');
      config.pairing.deviceToken = savedToken;
    } else {
      logger.info('Using device token from config');
      config.pairing.deviceToken = tokenToUse;
    }
  }

  // HTTP server (health/metrics)
  const httpServer = await createHttpServer(config);
  await httpServer.listen({ port: 8080 });
  logger.info('HTTP server listening on :8080');

  // WebSocket server
  const wsServer = new RelayWebSocketServer(config);
  await wsServer.start();

  // graceful shutdown
  const signals: NodeJS.Signals[] = ['SIGINT', 'SIGTERM'];
  for (const sig of signals) {
    process.on(sig, async () => {
      logger.info({ signal: sig }, 'Received shutdown signal');
      await wsServer.stop();
      await httpServer.close();
      process.exit(0);
    });
  }
}

main().catch((err) => {
  logger.fatal({ err }, 'Fatal error');
  process.exit(1);
});
```

---

### 12.4 shared/src/index.ts - 类型定义 (关键部分)

**文件**: `shared/src/index.ts`

```typescript
// ============================================================================
// Gateway Frame Types
// ============================================================================

export interface GatewayRequest {
  type: 'req';
  id: string;
  method: string;
  params: Record<string, unknown>;
}

export interface GatewayResponse {
  type: 'res';
  id: string;
  ok: boolean;
  payload?: unknown;
  error?: {
    code: string;
    message: string;
  };
}

export interface GatewayEvent {
  type: 'event';
  event: string;
  payload: unknown;
  seq?: number;
  stateVersion?: string;
}

export type GatewayFrame = GatewayRequest | GatewayResponse | GatewayEvent;

// ============================================================================
// Connect Params (✅ 已验证)
// ============================================================================

export type ClientId =
  | 'cli'
  | 'webchat'
  | 'test'
  | 'webchat-ui'
  | 'openclaw-control-ui'
  | 'openclaw-tui'
  | 'gateway-client'
  | 'openclaw-macos'
  | 'openclaw-ios'
  | 'openclaw-android'
  | 'node-host'
  | 'fingerprint'
  | 'openclaw-probe';

export type ClientMode = 'cli' | 'node' | 'webchat' | 'ui' | 'test' | 'backend' | 'probe';

/**
 * ✅ 已验证的 Connect Params
 *
 * 重要字段:
 * - role: 'operator' | 'node' | ...
 * - scopes: 操作权限列表
 * - device: 设备身份 (用于签名验证)
 *   - id: deviceId = SHA256(publicKey)
 *   - publicKey: 32字节原始公钥的 base64
 *   - signature: Ed25519 签名
 *   - signedAt: 时间戳 (毫秒)
 *   - nonce: 随机数 (必须与 challenge.nonce 一致)
 * - auth: 认证信息
 *   - token: Gateway 内部 token
 *   - deviceToken: 设备配对 token
 */
export interface ConnectParams {
  minProtocol: number;
  maxProtocol: number;
  client: {
    id: ClientId;
    displayName?: string;
    version: string;
    platform: string;
    deviceFamily?: string;
    modelIdentifier?: string;
    mode: ClientMode;
    instanceId?: string;
  };
  caps?: string[];
  commands?: string[];
  permissions?: Record<string, boolean>;
  pathEnv?: string;
  role?: string;
  scopes?: string[];
  device?: {
    id: string;
    publicKey: string;
    signature: string;
    signedAt: number;
    nonce: string;
  };
  auth?: {
    token?: string;
    bootstrapToken?: string;
    deviceToken?: string;
    password?: string;
  };
  locale?: string;
  userAgent?: string;
}

// ============================================================================
// Relay Config
// ============================================================================

export interface RelayConfig {
  port: number;
  host: string;
  tls: {
    enabled: boolean;
    keyPath?: string;
    certPath?: string;
  };
  gateway: {
    host: string;
    port: number;
  };
  pairing: {
    apiBase: string;
    deviceToken?: string;
  };
  heartbeat: {
    extIntervalMs: number;
    extTimeoutMs: number;
    gwIntervalMs: number;
    gwTimeoutMs: number;
  };
}
```

---

## 十三、错误日志汇总

### 13.1 首次连接 - nonce mismatch 错误

```
[INFO] Loaded device token from Gateway device-auth.json
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"

[INFO] Using saved device token

[INFO] WS connected to Gateway, waiting for challenge...
  host: "127.0.0.1"
  port: 18789

[INFO] Received connect.challenge, sending connect request...
  nonce: "4999fb05-ba30-476d-bee5-5fef767294ee"

[INFO] Loaded device key from Gateway identity (already paired)
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"

[ERROR] Gateway rejected connect
  error: {
    "code": "INVALID_REQUEST",
    "message": "device nonce mismatch",
    "details": {
      "code": "DEVICE_AUTH_NONCE_MISMATCH",
      "reason": "device-nonce-mismatch"
    }
  }
```

**原因**: websocket.ts 中使用了随机生成的 nonce，而不是 challenge 中的 nonce

---

### 13.2 修复 nonce 后 - signature invalid 错误

```
[INFO] Loaded device token from Gateway device-auth.json
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"

[INFO] Using saved device token

[INFO] WS connected to Gateway, waiting for challenge...
  host: "127.0.0.1"
  port: 18789

[INFO] Received connect.challenge, sending connect request...
  nonce: "4999fb05-ba30-476d-bee5-5fef767294ee"

[INFO] Loaded device key from Gateway identity (already paired)
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"

[ERROR] Gateway rejected connect
  error: {
    "code": "INVALID_REQUEST",
    "message": "device signature invalid",
    "details": {
      "code": "DEVICE_AUTH_SIGNATURE_INVALID",
      "reason": "device-signature"
    }
  }
```

**原因**: 设备密钥三元组 (deviceId / publicKey / privateKey) 不一致

---

### 13.3 配对流程 - WebSocket 意外关闭

```
[INFO] Starting WebSocket pairing flow
  wsUrl: "ws://127.0.0.1:18789"
  deviceId: "4b8b6b67c027aa62777c7efe0d5cea5878fd52a6655672eac862041b945507c3"

[INFO] WebSocket connected, waiting for challenge...

[INFO] Received connect.challenge
  nonce: "6cc6017b-8f9b-4e74-928d-208313e8e864"
  pairingAttempt: 0

[INFO] Attempt 0: Sending connect without device identity...

[INFO] Sending node.pair.request...

[INFO] WebSocket closed, waitingForPairing=true
```

**问题**: 发送 node.pair.request 后 WebSocket 立即关闭，没有收到响应

---

### 13.4 pairing.ts 日志 - 密钥加载

```
[INFO] Loaded existing device key from file
  deviceId: "4b8b6b67c027aa62777c7efe0d5cea5878fd52a6655672eac862041b945507c3"

# 删除旧密钥后

[INFO] Loaded device key from Gateway identity (already paired)
  deviceId: "2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4"
```

---

### 13.5 错误码对照表

| error.details.code | error.details.reason | 含义 | 可能原因 |
|-------------------|---------------------|------|---------|
| `DEVICE_AUTH_NONCE_MISMATCH` | `device-nonce-mismatch` | Nonce 不匹配 | 使用了错误的 nonce |
| `DEVICE_AUTH_SIGNATURE_INVALID` | `device-signature` | 签名无效 | 密钥不匹配或 payload 格式错误 |
| `DEVICE_AUTH_DEVICE_ID_MISMATCH` | `device-id-mismatch` | DeviceId 不匹配 | deviceId 与公钥不对应 |
| `DEVICE_AUTH_SIGNATURE_EXPIRED` | `device-signature-stale` | 签名过期 | signedAt 时间超出 5 分钟窗口 |
| `DEVICE_AUTH_NONCE_REQUIRED` | `device-nonce-missing` | Nonce 缺失 | 未提供 nonce |
| `DEVICE_IDENTITY_REQUIRED` | `device-identity-required` | 需要设备身份 | 首次连接需要配对 |

---

## 十四、调试验证命令

### 13.1 在服务器上执行 - 验证密钥一致性

```bash
# 1. 查看 Gateway 存储的设备信息
cat /root/.openclaw/identity/device.json
cat /root/.openclaw/identity/device-auth.json

# 2. 手动计算 deviceId 验证是否匹配
node -e "
const crypto = require('crypto');
const fs = require('fs');

// 读取 PEM 格式公钥
const pem = fs.readFileSync('/root/.openclaw/identity/device.json', 'utf-8');
const obj = JSON.parse(pem);
const publicKeyPem = obj.publicKeyPem;

// 从 PEM 提取 raw 32 字节公钥
const key = crypto.createPublicKey(publicKeyPem);
const spki = key.export({ type: 'spki', format: 'der' });
const raw = spki.slice(-32); // 去掉 SPKI header

// 计算 deviceId
const deviceId = crypto.createHash('sha256').update(raw).digest('hex');

console.log('=== 验证结果 ===');
console.log('Gateway deviceId:', obj.deviceId);
console.log('Computed deviceId:', deviceId);
console.log('Match:', obj.deviceId === deviceId ? '✅ YES' : '❌ NO');
console.log('');
console.log('Raw public key (base64):', raw.toString('base64'));
console.log('Raw public key length:', raw.length, 'bytes');
"
```

### 13.2 在服务器上执行 - 模拟签名验证

```bash
node -e "
const crypto = require('crypto');
const fs = require('fs');

// 读取设备密钥
const identity = JSON.parse(fs.readFileSync('/root/.openclaw/identity/device.json', 'utf-8'));
const auth = JSON.parse(fs.readFileSync('/root/.openclaw/identity/device-auth.json', 'utf-8'));

const privateKeyPem = identity.privateKeyPem;
const publicKeyPem = identity.publicKeyPem;
const deviceId = identity.deviceId;
const token = auth.tokens.operator.token;

// 模拟参数
const challengeNonce = 'test-nonce-12345';  // 需要从实际 challenge 获取
const signedAtMs = Date.now();
const scopes = 'operator.admin,operator.approvals,operator.pairing,operator.read,operator.talk.secrets,operator.write';

// 构建 V2 payload
const payload = \`v2|\${deviceId}|cli|cli|operator|\${scopes}|\${signedAtMs}|b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738|\${challengeNonce}\`;

console.log('=== 模拟签名 ===');
console.log('DeviceId:', deviceId);
console.log('Challenge nonce:', challengeNonce);
console.log('SignedAt:', signedAtMs);
console.log('Payload:', payload);
console.log('');

// 使用私钥签名
const sign = crypto.sign(null, Buffer.from(payload), privateKeyPem);
const signatureBase64 = sign.toString('base64');
console.log('Signature (base64):', signatureBase64);
console.log('');

// 使用公钥验证
const key = crypto.createPublicKey(publicKeyPem);
const sig = Buffer.from(signatureBase64, 'base64');
const ok = crypto.verify(null, Buffer.from(payload, 'utf8'), key, sig);
console.log('Verify result:', ok ? '✅ VALID' : '❌ INVALID');
"
```

### 13.3 获取当前 relay 实际使用的 deviceKey

在 `loadOrCreateDeviceKey()` 函数中添加日志：

```typescript
// 在 loadOrCreateDeviceKey() 返回前添加
logger.info({
  deviceId: deviceKey.deviceId,
  publicKey: deviceKey.publicKey,
  publicKeyLength: deviceKey.publicKey.length,
  privateKeyStartsWith: deviceKey.privateKey.substring(0, 30),
}, 'DEBUG: Loaded device key details');
```

### 13.4 对比表格

运行完 13.1 和 13.2 后，填入以下表格：

| 检查项 | Gateway 存储值 | Relay 计算值 | 是否匹配 |
|--------|--------------|-------------|---------|
| deviceId | `2dda14bf05401923ff2a52607616ed8be2704a6b7c5b7c85daa8b1ce80ce72a4` | `?` | ? |
| publicKey (前 20 字符) | `MCowBQYDK2VwAyEAG` | `?` | ? |
| 签名验证 | N/A | `✅ VALID` / `❌ INVALID` | - |

---

## 十五、参考资料

- OpenClaw Gateway 配对文档: https://docs.openclaw.ai/gateway/pairing.md
- OpenClaw Gateway 协议: https://docs.openclaw.ai/gateway/protocol.md
- 源码位置: `/Users/diccel/Downloads/AI研究资料/openclaw-main/src/gateway/`
  - 签名验证: `src/infra/device-identity.ts`
  - V2 payload: `src/gateway/device-auth.ts`
  - Gateway 验证: `src/gateway/server/ws-connection/message-handler.ts`
