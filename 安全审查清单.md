# 安全审查清单

> 版本：v1.0
> 日期：2026-04-26
> 状态：上线前必读

---

## 1. Token 存储安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Gateway Token 不提交到 GitHub | ✅ | 仅存服务器本地 `/etc/openclaw-relay/config.yaml` |
| Channel Token 不提交到 GitHub | ✅ | 仅存服务器本地或用户端 |
| PAT 不提交到 GitHub | ✅ | 仅在初始化时使用，后续用 SSH 部署密钥 |
| SSH 私钥不提交到 GitHub | ✅ | 存本地，`~.ssh/github_actions_deploy` |
| DEVICE_TOKEN 通过环境变量注入 | ✅ | `docker run -e DEVICE_TOKEN=...` |
| Extension Auth Token 加密存储 | ⚠️ 待审查 | `chrome.storage.local` 考虑使用 `session` storage |

## 2. CSP (Content Security Policy) 审查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| manifest.json CSP 配置正确 | ✅ | `connect-src` 仅允许 Relay 地址 |
| WebSocket 连接使用 WSS | ✅ | `wss://47.89.181.91:18790` |
| 图片资源来源限制 | ✅ | `img-src 'self' https: data: blob:` |
| 无内联脚本 | ✅ | `script-src 'self'` |

## 3. XSS 向量审查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 用户输入转义 | ✅ | Markdown 渲染使用 react-markdown |
| URL 链接安全 | ✅ | 图片 URL 需要安全检查（后续实现） |
| 消息内容不执行 | ✅ | 仅作为文本渲染 |

## 4. 依赖安全

```bash
npm audit
```

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 无高危漏洞 | ⚠️ 5 moderate | 需上线前再次确认 |
| 已知漏洞已评估 | ✅ | glob@7.2.3 已知问题，不影响 Chrome Extension |

## 5. 网络安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Relay ↔ Gateway 仅本地连接 | ✅ | `127.0.0.1:18789` |
| Extension → Relay 使用 WSS (TLS) | ✅ | 公网暴露端口 18790 |
| 无其他内网服务暴露 | ✅ | 阿里云安全组仅开放必要端口 |

## 6. 部署安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| Docker 容器以非 root 用户运行 | ⚠️ 待配置 | 考虑添加 `USER` 指令 |
| 容器镜像最小化 | ✅ | 使用 `node:22-alpine` |
| 无明文密码在容器内 | ✅ | Token 通过环境变量注入 |

---

## 上线前操作项

- [ ] 运行 `npm audit` 确认无高危漏洞
- [ ] 确认 GitHub Secrets 全部配置正确
- [ ] 确认阿里云安全组仅开放 18790 (WSS) 端口
- [ ] 配置 Docker 以非 root 用户运行
- [ ] 确认 Extension 在 Chrome Web Store 或开发者模式可正常加载
- [ ] 测试完整对话流程
- [ ] 测试网络闪断重连
- [ ] 测试会话持久化（刷新后消息是否恢复）
