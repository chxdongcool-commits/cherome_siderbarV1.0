# 项目任务计划

> 版本：v1.1
> 日期：2026-04-26
> 状态：Phase 1 待启动

---

## 概述

本项目为 **OpenClaw Sidebar Chrome Extension**，包含 Extension 侧边栏和 Relay Server 中继服务，通过 WebSocket 实现浏览器与 OpenClaw Gateway 的实时双向通信。

**技术架构**：Chrome Extension (Side Panel) ↔ WSS ↔ Relay Server (Node.js) ↔ WS ↔ OpenClaw Gateway

**仓库地址**：`https://github.com/chxdongcool-commits/cherome_siderbarV1.0`

---

## ⚠️ 重要：协议细节待开发验证

以下为基于 OpenClaw 文档的**推测设计**，实际协议字段（事件名、payload 结构）需在 Phase 2/3 开发过程中通过**抓包验证**确认，并同步更新技术文档：

| 待验证项 | 验证时机 | 备注 |
|----------|----------|------|
| Gateway 事件具体字段名（`session.message.delta` 等） | Phase 2/3 开发时 | 事件名可能与推测不同 |
| `session.message.delta` 的 payload 结构 | Phase 2/3 开发时 | delta 可能是 string 或 object |
| Snapshot 快照的完整 schema | Phase 2 开发时 | `hello-ok` 事件格式 |
| 两套订阅的具体 RPC 响应格式 | Phase 2 开发时 | `sessions.subscribe` / `sessions.messages.subscribe` |
| `hello-ok` 的 `policy` 字段详情 | Phase 2 开发时 | 心跳间隔等配置 |

---

## 开发阶段总览

| 阶段 | 周期 | 任务数 | 核心目标 |
|------|------|--------|----------|
| Phase 1 | — | 7 | 初始化项目脚手架 |
| Phase 2 | 1-2 周 | 6 | Relay Server 核心功能 |
| Phase 3 | 1-2 周 | 5 | Extension 骨架 |
| Phase 4 | 2-3 周 | 6 | 核心功能开发 |
| Phase 5 | 1-2 周 | 4 | 上线准备 |
| **合计** | **5-9 周** | **28** | — |

---

## Phase 1：初始化项目

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 9 | 初始化 Monorepo 目录结构 | pending | 创建 extension/、relay-server/、shared/、docs/、.github/workflows/，初始化 workspace 根 package.json |
| 10 | 创建 shared 类型定义包 | pending | 定义 Extension 与 Relay 共用的 TypeScript 类型（RPC Request/Response/Event 三层帧格式、StreamingEvent、SessionEvent、ConnectionState 等），注意：实际类型需在开发时抓包确认 |
| 11 | 初始化 Relay Server 脚手架 | pending | Node.js 22 + ws + fastify + TypeScript，包含 main.ts、config.ts、logger.ts、Dockerfile、ecosystem.config.js |
| 13 | 初始化 Extension 脚手架 | pending | Vite + React 18 + TypeScript，包含 vite.config.ts、Manifest V3 基础配置、.gitignore |
| 12 | 创建 GitHub Actions CI/CD 流水线 | pending | ci.yml（lint + typecheck + test + build）、cd.yml（Tag 触发部署） |
| 15 | 编写 README.md | pending | 项目简介、目录结构、本地开发指南、Docker 部署说明、CI/CD 说明、贡献规范 |
| 14 | 推送代码到 GitHub | pending | 将所有脚手架代码推送到 chxdongcool-commits/cherome_siderbarV1.0 |

---

## Phase 2：Relay Server 开发

> ⚠️ Relay Server 是与 Gateway 协议交互的核心层。实际 RPC 方法名、事件名、payload 结构依赖开发时抓包验证，技术文档中的类型定义为推测值。

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 18 | WebSocket 服务器 | pending | 监听 18791 端口、接收 Extension 连接、管理连接生命周期、协议版本路由、错误处理 |
| 17 | 认证模块 | pending | 校验 Extension Token（JWT）、连接 Gateway 时携带 Gateway Token、Token 过期处理、Relay Token 刷新接口（/v1/refresh） |
| 19 | 消息转发模块 | pending | Extension ↔ Relay ↔ Gateway 双向消息转发、RPC Request/Response/Event 三层帧格式转换、GatewayEvent 事件透传、两套订阅机制（sessions.subscribe + sessions.messages.subscribe） |
| 21 | 心跳与重连策略 | pending | Extension ↔ Relay 心跳（20s/60s）、Relay ↔ Gateway 心跳（15s/45s）、指数退避重连、Gateway 断连检测与恢复 |
| 16 | HTTP 管理接口 | pending | /health 健康检查、/metrics 关键指标、/v1/logs 日志上报、/v1/crash 崩溃上报 |
| 20 | 部署 Relay Server 到阿里云 | pending | docker-compose.yml 部署、配置 Nginx WSS 透传（18790 → 18791）、验证 Relay 与 Gateway 连通性 |

---

## Phase 3：Extension 骨架

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 22 | Manifest V3 配置 | pending | sidePanel 入口、Service Worker 配置、permissions、CSP、commands 快捷键（Alt+Shift+O）、host_permissions |
| 23 | Service Worker 连接管理 | pending | 唯一 WSS 长连接、懒连接模式、连接缓存、连接状态维护、Extension ↔ SW 消息总线 |
| 26 | 首次连接与认证流程 | pending | connect → register → ready 握手时序、Token 存储（chrome.storage.local）、连接失败处理与重试 |
| 25 | Side Panel UI 骨架 | pending | 标题栏（会话名称/新建按钮）、消息列表组件、输入框组件、基础布局（React + TypeScript） |
| 24 | 单元测试 | pending | Vitest 单元测试覆盖连接管理、消息订阅状态机、存储抽象、异常处理，覆盖率 > 70% |

---

## Phase 4：核心功能

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 27 | 流式块输出渲染 | pending | `session.message.delta` 增量渲染、`session.message.start/end` 收尾、seq 乱序处理、pending buffer 维护、Markdown 增量解析、闪烁光标 |
| 31 | Markdown 渲染 | pending | react-markdown + remark-gfm + rehype-highlight 代码高亮、表格横向滚动、一键复制、链接新标签页、图片安全检查 |
| 29 | Typing Indicator | pending | 四种 typingMode 行为（instant/thinking/message/never）、`typing.start/end` 事件处理、`tick` liveness 信号刷新、12s 保底隐藏机制 |
| 32 | 会话持久化与恢复 | pending | IndexedDB 存储消息（messages_<sessionId>）、chrome.storage.local 存储 sessionMetaList/activeSessionId、会话列表 UI |
| 30 | 新建会话与切换 | pending | `sessions.create` 流程、UUIDv4 生成 sessionId、两套订阅（sessions.subscribe + sessions.messages.subscribe）、本地消息列表清空、旧会话保留在历史列表 |
| 28 | 异常处理与重连策略 | pending | 指数退避重连（1s→30s）、网络闪断处理、Gateway 重启后重连握手（含 hello-ok snapshot 恢复）、wake from sleep 处理 |

---

## Phase 5：上线准备

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 34 | 端到端测试 | pending | Playwright E2E 测试：完整对话流程、会话恢复流程、网络异常重连场景验证 |
| 33 | 完善 CD 流水线 | pending | Tag 触发构建 Extension zip、推送 Docker 镜像到阿里云 ACR、SSH 部署脚本、钉钉/飞书通知 |
| 35 | 上线前安全审查 | pending | Token 存储审查、CSP 审查、XSS 向量审查、npm audit 无高危漏洞、代码审查 checklist |
| 36 | 编写上线文档和使用手册 | pending | 部署文档（生产环境）、使用手册（用户侧）、常见问题排查、监控告警配置 |

---

## 任务依赖关系

```
Phase 1
  └── #9 Monorepo 目录结构
        ├── #10 shared 类型定义包
        ├── #11 Relay Server 脚手架
        ├── #13 Extension 脚手架
        ├── #12 CI/CD 流水线
        │     ├── #11（Relay build）
        │     └── #13（Ext build）
        └── #15 README.md
              └── #14 推送 GitHub

Phase 2（依赖 Phase 1 完成）
  #20 部署 Relay Server（依赖 #18 #17 #19 #21 #16）

Phase 3（依赖 Phase 2 完成）
  （Relay Server 部署成功后才能测试 Extension 连接）

Phase 4（依赖 Phase 3 完成）
  （Extension 骨架完成后才能开发核心功能）

Phase 5（依赖 Phase 4 完成）
  （所有功能开发完成后才能上线）
```

---

## 进度更新日志

| 日期 | 更新内容 |
|------|----------|
| 2026-04-26 | 初始化任务清单，建立 36 个任务，分 5 个阶段 |
| 2026-04-26 | v1.1 更新：补充协议待验证说明、snapshot streaming 解释、流式事件名修正（message_start→session.message.delta）、两套订阅机制说明、hello-ok snapshot 处理 |
| 2026-04-26 | Phase 2 完成: 配对模块(pairing.ts)、WebSocket服务器(websocket.ts)、消息转发(relay.ts)、心跳重连、HTTP管理接口、部署配置(Docker/CD) |
