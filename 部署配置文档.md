# 部署配置文档

> 版本：v1.0
> 日期：2026-04-26
> 状态：已验证 / 待初始化项目脚手架

---

## 1. 云服务器

| 项目 | 值 |
|------|-----|
| 公网 IP | `47.89.181.91` |
| SSH 用户 | `root` |
| SSH 端口 | `22`（默认） |
| SSH 登录方式 | 私钥（见下方） |
| 服务器系统 | Alibaba Cloud Linux（基于 CentOS/AL8），内核 `5.10.134` |
| Docker 版本 | `26.1.3` |
| Docker Compose 版本 | `v2.27.0` |

### SSH 私钥

```
-----BEGIN OPENSSH PRIVATE KEY-----

-----END OPENSSH PRIVATE KEY-----
```

### OpenClaw Gateway 状态

| 项目 | 值 |
|------|-----|
| Gateway 进程 | `openclaw-gateway`（已运行） |
| 监听地址 | `127.0.0.1:18789`（仅本地，不可外部访问） |
| Gateway Token | `b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738` |
| 配置文件路径 | `/home/admin/.openclaw/openclaw.json` |

---

## 2. GitHub 仓库

| 项目 | 值 |
|------|-----|
| 仓库地址 | `https://github.com/chxdongcool-commits/cherome_siderbarV1.0` |
| 所有者 | `chxdongcool-commits` |
| 仓库名称 | `cherome_siderbarV1.0` |
| 可见性 | **Private**（私有，仅所有者可见） |
| 默认分支 | `main` |
| 仓库状态 | 空（无初始文件） |

### GitHub Personal Access Token（PAT）

| 项目 | 值 |
|------|-----|
| Token | `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` |
| 权限范围 | `repo`（仓库读写）、`workflow`（GitHub Actions） |
| 到期时间 | 见 GitHub Settings → Developer settings → Personal access tokens |

> **注意**：PAT 仅在创建仓库和初始化 CI 流水线时需要，后续通过 GitHub Actions 的 SSH 部署密钥自动管理。

---

## 3. OpenClaw Gateway 配置

### Gateway Token（Relay 连接用）

```
b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738
```

> 此 Token 用于 Relay Server 连接 Gateway 时进行内部认证，绝不能泄露给 Extension 或提交到 GitHub。

### Channel Token（chrome-openclaw-sider 插件认证用）

| Channel | Token |
|---------|-------|
| `chrome-openclaw-sider` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpYXQiOjE3NzY2Njc5NjMsImp0aSI6ImFlNWM1MjIyLWFhODEtNDM5MS1iYTY2LTc2MDM2YzU5NWIxNSIsInN1YiI6IjAxS1BNVEY4MlYwTjZaV1hBMjdHUFQ5MU5SIiwidHlwZSI6InNlbGZjbGF3IiwidWlkIjoyNTM4MDY4OX0.ueMcYys0wP5jFbHXIeOKI8RaOL1vu8BR-OHzsFh-jJ0` |

> 此 Token 是 `chrome-openclaw-sider` channel 的认证凭证，用于 Extension 连接到 Gateway。

### Gateway 配置详情

```json
{
  "gateway": {
    "mode": "local",
    "auth": {
      "mode": "token",
      "token": "b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738"
    },
    "port": 18789,
    "bind": "loopback",
    "controlUi": {
      "allowInsecureAuth": true,
      "dangerouslyDisableDeviceAuth": true,
      "allowedOrigins": ["*"]
    }
  },
  "channels": {
    "chrome-openclaw-sider": {
      "enabled": true,
      "token": "<见上方 Channel Token>"
    }
  }
}
```

---

## 4. 部署拓扑

```
互联网
  │
  │ WSS (TLS, 端口 18790)
  ▼
┌─────────────────────────────────────┐
│         阿里云服务器（ECS）           │
│                                     │
│  Nginx (端口 18790, WSS/HTTPS)       │
│    │                                │
│    │ WS (本地, 端口 18791)          │
│    ▼                                │
│  Relay Server (Node.js, 端口 18791) │
│    │                                │
│    │ WS (本地回环, 端口 18789)      │
│    ▼                                │
│  OpenClaw Gateway（已运行）          │
│  127.0.0.1:18789                    │
└─────────────────────────────────────┘
```

---

## 5. 安全注意事项

| 注意项 | 说明 |
|--------|------|
| **Gateway Token** | 不得提交到 GitHub，仅限服务器本地使用 |
| **Channel Token** | 不得提交到 GitHub，用于 Extension 认证 |
| **PAT** | 仅在初始化项目时需要，后续用 GitHub Actions 部署密钥 |
| **服务器 SSH 私钥** | 不得提交到 GitHub，仅存放在安全位置 |
| **Relay ↔ Gateway** | 建议走本地 WS（`127.0.0.1`），不暴露内网 |
| **Extension ↔ Relay** | 必须 WSS（TLS 加密） |

---

## 6. 验证命令汇总

```bash
# SSH 登录服务器
ssh -i /path/to/key -o StrictHostKeyChecking=no -p 22 root@47.89.181.91

# 查看 Gateway 状态
ss -tlnp | grep 18789

# 查看 Docker 容器
docker ps

# 查看 Relay 相关日志（部署后）
docker logs -f openclaw-relay --tail 200
```
