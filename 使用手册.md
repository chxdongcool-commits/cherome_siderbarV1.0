# OpenClaw Sidebar 使用手册

> 版本：v1.0
> 日期：2026-04-26

---

## 功能概览

OpenClaw Sidebar 是一个 Chrome 扩展，让你在浏览器侧边栏直接与 OpenClaw AI 对话。

**主要功能：**
- 🌐 通过侧边栏与 AI 对话
- 💬 流式输出，实时显示 AI 回复
- 📝 Markdown 渲染（代码高亮、表格、列表等）
- 💾 会话持久化（刷新不丢失）
- 🔄 自动重连（网络波动时）
- ⌨️ 快捷键 `Alt+Shift+O` 打开侧边栏

---

## 安装扩展（开发者模式）

1. 下载或克隆本仓库
2. 构建扩展：
   ```bash
   npm install
   npm run build
   ```
3. 打开 Chrome，访问 `chrome://extensions/`
4. 开启右上角 **开发者模式**
5. 点击 **加载已解压的扩展程序**
6. 选择 `extension/dist` 文件夹

---

## 使用步骤

### 1. 打开侧边栏

方式一：点击 Chrome 工具栏右侧的扩展图标
方式二：使用快捷键 `Alt+Shift+O`

### 2. 建立连接

扩展启动后会自动连接服务器。等待状态指示器变为绿色 **Connected**。

### 3. 开始对话

- 输入消息，按 `Enter` 或点击 **Send** 发送
- 支持多行输入（`Shift+Enter` 换行）
- AI 回复会实时流式显示

### 4. 管理会话

- 点击左上角 **☰** 打开会话列表
- 查看所有历史会话
- 点击 **+ New** 创建新会话
- 点击任意会话切换

### 5. 消息渲染

AI 回复支持：
- **粗体**、*斜体*
- `行内代码` 和代码块（语法高亮）
- 表格
- 引用
- 链接
- 列表

---

## 故障排查

### 连接失败

1. 检查网络是否正常
2. 确认 Relay 服务器是否运行（`http://服务器IP:18791/health`）
3. 刷新扩展（`chrome://extensions/` → 刷新按钮）

### 消息发送失败

1. 检查连接状态是否为 Connected
2. 尝试重新打开侧边栏
3. 刷新扩展

### 扩展无法加载

1. 确认 `dist` 目录下有 `manifest.json`
2. 检查是否开启了开发者模式
3. 尝试重新加载

---

## 数据存储

- **会话列表**：存储在 IndexedDB（浏览器本地）
- **消息历史**：存储在 IndexedDB（浏览器本地）
- **认证 Token**：存储在 `chrome.storage.local`（浏览器本地）

所有数据仅存储在本地，不会上传到任何服务器。

---

## 隐私说明

- 你的对话数据仅通过 Relay 服务器转发给 OpenClaw Gateway
- 不会存储在第三方服务器
- 所有传输使用 WebSocket over TLS（WSS）加密

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Alt+Shift+O` | 打开侧边栏 |

---

## 卸载

1. 打开 `chrome://extensions/`
2. 找到 **OpenClaw Sidebar**
3. 点击 **移除**

---

## 反馈与支持

如遇问题，请通过以下方式反馈：
- GitHub Issues: https://github.com/chxdongcool-commits/cherome_siderbarV1.0/issues
