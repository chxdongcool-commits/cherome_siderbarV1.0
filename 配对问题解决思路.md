# OpenClaw Gateway 配对问题解决思路

> 版本：v2.0
> 日期：2026-04-27
> 状态：✅ 已解决

---

## 核心问题

`DEVICE_AUTH_SIGNATURE_INVALID - device-signature` 签名验证失败。

---

## 问题根因

### 关键认知：Device / Operator / Node 三套体系

```
devices   （设备身份：key + token）
nodes     （可运行节点：需要审批）
operators （调用管理接口的权限语义）
```

| 身份 | 作用 | 权限 |
|------|------|------|
| Device | 证明"我是谁" | ❌ 不能 pair |
| Operator | 管理权限 | ✅ node.pair.request |
| Node | 运行实例 | ✅ connect, report session |

### 真正的问题

Gateway 在内部做的是：

```ts
if (!connection.isAuthenticated) close()
if (!connection.hasScope("operator.pairing")) close()
```

**不是 UI 登录，而是 token 解析出的 role + scopes**

---

## 修复方案

### 方案 A（推荐）：使用 Operator Token

```
1. 用 operator token 连接 Gateway
2. 调用 node.pair.request
3. 审批
4. node 用 device identity 连接
```

### 方案 B（手动审批）

```bash
openclaw nodes approve <deviceId>
```

---

## 修复内容

### 1. pairing.ts 修改

**修改前**：
```typescript
auth: {
  token: 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738',  // Gateway token ❌
}
```

**修改后**：
```typescript
auth: {
  token: 'fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU',  // Operator token ✅
}
```

### 2. websocket.ts 修改

**修改前**：
```typescript
const GATEWAY_TOKEN = 'b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738';
const payload = `v2|${deviceKey.deviceId}|cli|cli|operator|${scopes}|${signedAt}|${GATEWAY_TOKEN}|${challengePayload.nonce}`;
// ...
auth: {
  deviceToken: this.config.pairing.deviceToken,  // 错误字段 ❌
}
```

**修改后**：
```typescript
const OPERATOR_TOKEN = 'fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU';
const payload = `v2|${deviceKey.deviceId}|cli|cli|operator|${scopes}|${signedAt}|${OPERATOR_TOKEN}|${challengePayload.nonce}`;
// ...
auth: {
  token: OPERATOR_TOKEN,  // 正确字段 ✅
}
```

---

## 验证结果

```
[2026-04-27 00:16:08.140 +0800] INFO: Received hello-ok, Gateway handshake complete
    connId: "bebcbb02-8d73-4998-b462-2bee6221df06"
    methods: 121
[2026-04-27 00:16:08.141 +0800] INFO: Relay WebSocket server started
    port: 18791
```

---

## 一句话总结

> **设备签名 payload 中的 token 必须与 auth.token 字段一致，且都使用 Operator token，不是 Gateway token。**

---

## 相关文件

- `relay-server/src/server/pairing.ts` - 配对流程
- `relay-server/src/server/websocket.ts` - Gateway 连接

---

## 参考

- Gateway token: `b5cd1af01f5a5330ddf36554a080a5ee887799f75648a738` (设备级)
- Operator token: `fSWST0KzO4UPwwCfvsiObkHXeKCyiY2GLWueN7s0psU` (管理级)
- 设备 identity: `~/.openclaw/identity/device.json`
- Operator tokens: `~/.openclaw/identity/device-auth.json`
